{
  "name": "tree-sitter-git-conventional-commit-subject",
  "version": "1.0.0",
  "description": "a tree-sitter parser for conventional commit subjects",
  "main": "grammar.js",
  "scripts": {
    "build": "rm -rf ./src ./bindings && tree-sitter generate",
    "test": "tree-sitter test",
    "build-wasm": "tree-sitter build-wasm",
    "playground": "tree-sitter playground",
    "cargo-build": "cargo build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tree-sitter-cli": "^0.20.7"
  },
  "dependencies": {
    "nan": "^2.17.0"
  }
}
